[build]
  command = "npm run build && npx puppeteer browsers install chrome && node scripts/prerender.mjs"
  publish = "dist"

# SPA fallback — serve index.html for all routes without a static file
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
